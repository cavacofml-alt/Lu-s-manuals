# Egress matrix

Generated by `python evaluation/harness/egress_matrix.py`, which exercises the real
`EgressGuard`. Regenerate after any change to providers or policy; do not hand-edit.

Last generated against commit `6dcfb0e`.

```

=== EGRESS_POLICY_MODE=strict_local ==============================================
capability    provider              where   PUBLIC         INTERNAL       CONFIDENTIAL   HIGHLY_CONFID  
embedding     voyage-3              cloud   BLOCKED        BLOCKED        BLOCKED        BLOCKED        
embedding     bge-m3                local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
llm           claude-opus-5         cloud   BLOCKED        BLOCKED        BLOCKED        BLOCKED        
llm           local-llm             local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
rerank        cloud-reranker        cloud   BLOCKED        BLOCKED        BLOCKED        BLOCKED        
rerank        bge-reranker-v2-m3    local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
vision        claude-vision         cloud   BLOCKED        BLOCKED        BLOCKED        BLOCKED        
vision        local-vlm             local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
ocr           cloud-ocr             cloud   BLOCKED        BLOCKED        BLOCKED        BLOCKED        
ocr           tesseract             local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        

=== EGRESS_POLICY_MODE=mixed ==============================================
capability    provider              where   PUBLIC         INTERNAL       CONFIDENTIAL   HIGHLY_CONFID  
embedding     voyage-3              cloud   ALLOWED        ALLOWED        BLOCKED        BLOCKED        
embedding     bge-m3                local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
llm           claude-opus-5         cloud   ALLOWED        ALLOWED        BLOCKED        BLOCKED        
llm           local-llm             local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
rerank        cloud-reranker        cloud   ALLOWED        ALLOWED        BLOCKED        BLOCKED        
rerank        bge-reranker-v2-m3    local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
vision        claude-vision         cloud   ALLOWED        ALLOWED        BLOCKED        BLOCKED        
vision        local-vlm             local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
ocr           cloud-ocr             cloud   ALLOWED        ALLOWED        BLOCKED        BLOCKED        
ocr           tesseract             local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        

=== EGRESS_POLICY_MODE=permissive ==============================================
capability    provider              where   PUBLIC         INTERNAL       CONFIDENTIAL   HIGHLY_CONFID  
embedding     voyage-3              cloud   ALLOWED        ALLOWED        BLOCKED        BLOCKED        
embedding     bge-m3                local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
llm           claude-opus-5         cloud   ALLOWED        ALLOWED        BLOCKED        BLOCKED        
llm           local-llm             local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
rerank        cloud-reranker        cloud   ALLOWED        ALLOWED        BLOCKED        BLOCKED        
rerank        bge-reranker-v2-m3    local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
vision        claude-vision         cloud   ALLOWED        ALLOWED        BLOCKED        BLOCKED        
vision        local-vlm             local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        
ocr           cloud-ocr             cloud   ALLOWED        ALLOWED        BLOCKED        BLOCKED        
ocr           tesseract             local   ALLOWED        ALLOWED        ALLOWED        ALLOWED        

=== startup validation ==================================================
  BOOTS            shipped defaults (local emb + Claude) claiming internal
  STARTUP FAILURE  shipped defaults claiming confidential
  BOOTS            all local, claiming highly_confidential
  STARTUP FAILURE  strict_local with one cloud provider present
  BOOTS            strict_local, fully local

PROPERTY HOLDS
```
